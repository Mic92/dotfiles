# Afew Configuration

This directory contains the afew configuration for email tagging and processing.

## Project Structure

- `config` - Afew configuration file with tag filters and mail mover rules
- `shell.nix` - Development environment
