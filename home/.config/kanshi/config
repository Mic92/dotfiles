# Profiles are generated by the noctalia display-config panel; that file
# is rewritten wholesale on every save, so add hand-written profiles below
# the include if needed.
include ~/.config/kanshi/noctalia.conf
