# Coder workspace integration — install.sh normally appends a source line to
# ~/.tmux.conf, but homesick replaces that file with this symlink. Source
# coder's config first so SSH_AUTH_SOCK persistence and sandbox tmux.conf
# loading work, then let our own config override styling below.
if-shell "test -r /root/code/config/remote/tmux.conf" \
  "source-file /root/code/config/remote/tmux.conf"

set -g mouse on
set -g set-titles on
set-option -g set-titles-string "#H"

bind-key -n WheelUpPane \
    if-shell -Ft= "#{?pane_in_mode,1,#{mouse_button_flag}}" \
        "send-keys -M" \
        "if-shell -Ft= '#{alternate_on}' \
            'send-keys Up Up Up' \
            'copy-mode'"

bind-key -n WheelDownPane \
    if-shell -Ft= "#{?pane_in_mode,1,#{mouse_button_flag}}" \
        "send-keys -M" \
        "send-keys Down Down Down"

# jump to last prompt
bind-key -T copy-mode-vi [ send-keys -X previous-prompt
bind-key -T copy-mode-vi ] send-keys -X next-prompt

# sync tmux buffer with terminal clipboard
set -g set-clipboard on
unbind f
set -g @thumbs-key f
set -g @thumbs-command "command -v xdg-open {} && xdg-open {} || command -v open && open {}"
set -g @thumbs-upcase-command "tmux set-buffer -w -- {} && tmux paste-buffer"
# solarized light theme
set -g @thumbs-bg-color '#657b83'
set -g @thumbs-fg-color '#fdf6e3'
set -g @thumbs-hint-bg-color '#b58900'
set -g @thumbs-hint-fg-color '#fdf6e3'
set -g @thumbs-select-bg-color '#268bd2'
set -g @thumbs-select-fg-color '#fdf6e3'
set -g @thumbs-contrast 1
set -g @thumbs-unique 1
set -g @thumbs-reverse enabled
# match sri und sha256 hashes for nix
set -g @thumbs-regexp-1 '(sha256-[0-9a-zA-z=/+]{44}|[0-9a-f]{7,40}|[0-9a-z]{52})'
run-shell ~/.nix-profile/share/tmux-plugins/tmux-thumbs/tmux-thumbs.tmux

# allow passing through escape sequences
set -g allow-passthrough on

# extended keys for proper modifier key forwarding (Shift+Enter, Ctrl+Enter, etc.)
set -g extended-keys on
set -g extended-keys-format csi-u

# address vim mode switching delay (http://superuser.com/a/252717/65504)
set -s escape-time 0

# increase scrollback buffer size
set -g history-limit 50000

# tmux messages are displayed for 4 seconds
set -g display-time 4000

# refresh 'status-left' and 'status-right' more often
set -g status-interval 5

# upgrade $TERM
set -g default-terminal 'tmux-256color'
set-option -sa terminal-overrides ",*:Tc"
# clickable osc-8 hyperlinks
set -ga terminal-features "*:hyperlinks"

# emacs key bindings in tmux command prompt (prefix + :) are better than
# vi keys, even for vim users
set -g status-keys emacs

# focus events enabled for terminals that support them
set -g focus-events on

# super useful when using "grouped sessions" and multi-monitor setup
setw -g aggressive-resize on

# vi-style controls for copy mode
setw -g mode-keys vi

# Prefix key.
unbind C-b
unbind C-Space
set -g prefix C-Space
bind C-Space send-prefix

# confirm before killing a window or the server
bind-key k confirm kill-window
bind-key K confirm kill-server

# Styles the active pane border. Helps when you have
# more than two panes.
set-option -g renumber-windows on

set -g visual-activity on

# start window indexing at one instead of zero
set -g base-index 1

# Use vim keybindings in copy mode
setw -g mode-keys vi

set -g update-environment 'DISPLAY SSH_ASKPASS SSH_AGENT_PID SSH_AUTH_SOCK SSH_CONNECTION SSH_TTY WINDOWID XAUTHORITY TERM UPTERM_ADMIN_SOCKET'

# keep working directory in new tabs
bind '"' split-window -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"

set-option -g set-clipboard on

# sesh: smart tmux session manager (zoxide + tmux sessions + configs)
# https://github.com/joshmedeski/sesh#recommended-tmux-settings
set -g detach-on-destroy off  # don't exit tmux when closing a session
bind-key x kill-pane          # skip "kill-pane 1? (y/n)" prompt

bind-key s run-shell "sesh connect \"$(
  sesh list --icons | fzf-tmux -p 80%,70% \
    --no-sort --ansi --border-label ' sesh ' --prompt '⚡  ' \
    --header '  ^a all ^t tmux ^x zoxide ^w workmux ^d kill ^f find' \
    --bind 'tab:down,btab:up' \
    --bind 'ctrl-a:change-prompt(⚡  )+reload(sesh list --icons)' \
    --bind 'ctrl-t:change-prompt(🪟  )+reload(sesh list -t --icons)' \
    --bind 'ctrl-x:change-prompt(📁  )+reload(sesh list -z --icons)' \
    --bind 'ctrl-w:change-prompt(🌳  )+reload(cd \"#{pane_current_path}\" && workmux list --json | jq -r \".[].path\")' \
    --bind 'ctrl-f:change-prompt(🔎  )+reload(fd -H -d 2 -t d -E .Trash . ~)' \
    --bind 'ctrl-d:execute(tmux kill-session -t {2..})+change-prompt(⚡  )+reload(sesh list --icons)' \
    --preview-window 'right:55%' \
    --preview 'sesh preview {}'
)\""
# Old choose-tree still reachable for when sesh/fzf misbehave.
bind-key S choose-tree -Zs

bind -N "last-session (via sesh)" L run-shell "sesh last"
bind -N "switch to root session (via sesh)" 9 run-shell "sesh connect --root '#{pane_current_path}'"

# workmux dashboard popup
bind C-s display-popup -h 30 -w 100 -E "workmux dashboard"
bind C-w display-popup -h 30 -w 100 -E "workmux dashboard --tab worktrees"

bind R run "echo \"stty columns $(tmux display -p \#{pane_width}); stty rows $(tmux display -p \#{pane_height})\" | tmux load-buffer - ; tmux paste-buffer"

## Tokyo Night (night) status bar — palette from folke/tokyonight.nvim extras/tmux
set -g focus-events on
set -g status-justify absolute-centre
set -g status-left-length 90
set -g status-right-length 90

set -g mode-style 'fg=#7aa2f7,bg=#3b4261'
set -g message-style 'fg=#7aa2f7,bg=#3b4261'
set -g message-command-style 'fg=#7aa2f7,bg=#3b4261'
set -g pane-border-style 'fg=#3b4261'
set -g pane-active-border-style 'fg=#7aa2f7'
set -g status-style 'fg=#7aa2f7,bg=#16161e'

setw -g window-status-separator ''
setw -g window-status-style 'NONE,fg=#a9b1d6,bg=#16161e'
setw -g window-status-activity-style 'underscore,fg=#e0af68,bg=#16161e'

set-option -gq status-left '#[fg=#15161e,bg=#7aa2f7,bold] #S #{prefix_highlight}#[fg=#7aa2f7,bg=#16161e,nobold,nounderscore,noitalics]'
# @host_color is set per-host from zshrc (string_hash $HOST) so remote sessions
# stay visually distinct while the rest of the bar keeps the Tokyo Night palette.
set-option -gq @host_color '#7aa2f7'
set-option -gq status-right '#[fg=#{@host_color},bg=#16161e]#[fg=#15161e,bg=#{@host_color},bold] #h '
set-option -gq window-status-format '#[fg=#16161e,bg=#16161e]#[fg=#565f89,bg=#16161e] #I  #W #[fg=#16161e,bg=#16161e]'
set-option -gq window-status-current-format '#[fg=#16161e,bg=#3b4261]#[fg=#7aa2f7,bg=#3b4261,bold] #I  #W #[fg=#3b4261,bg=#16161e,nobold]'

set -g @prefix_highlight_output_prefix '#[fg=#e0af68]#[bg=#16161e]#[fg=#16161e]#[bg=#e0af68]'
set -g @prefix_highlight_output_suffix ''
