[package]
name = "fluent-bit-journal-filter"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
nanoserde = "0.2"

[profile.release]
opt-level = "s"
lto = true
strip = true
