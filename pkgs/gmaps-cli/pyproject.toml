[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gmaps-cli"
version = "0.1.0"
description = "CLI tool to manage Google Maps lists and places"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
gmaps-cli = "gmaps_cli:main"

[tool.hatch.build.targets.wheel]
packages = ["gmaps_cli.py"]
