#!/usr/bin/env python
import sys
import os

sys.path.insert(
    0, os.path.join(os.path.dirname(os.path.dirname(os.path.realpath(__file__))))
)

from nix_uci import main  # NOQA

if __name__ == "__main__":
    main()
