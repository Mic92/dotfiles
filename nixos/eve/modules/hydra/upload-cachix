#!/usr/bin/env python

import json
import shutil
import os


def main() -> None:
    print('hello world')
    shutil.copyfile(os.environ["HYDRA_JSON"], "/tmp/hydra.json")
    with open(os.environ["HYDRA_JSON"]) as f:
        data = json.load(f)


if __name__ == "__main__":
    main()
